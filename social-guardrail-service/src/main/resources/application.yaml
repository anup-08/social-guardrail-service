spring:
  application:
    name: social-guardrail-service

  datasource:
    url: jdbc:postgresql://localhost:5432/social_guardrail_db
    username: postgres
    password: password123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        jdbc:
          time_zone: UTC


  data:
    redis:
      host: localhost
      port: 6379

